# Backend-Festival-Booth

## 環境構築

Go をインストールしろ

## 開発の流れ

docker コンテナを起動

```
docker compose up -d
```

docker コンテナを再起動 (ソースコード修正時など) air が効くなら要らない

```
docker compose restart
```

docker コンテナを閉じる

```
docker compose down
```
